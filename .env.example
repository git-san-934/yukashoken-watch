# EDINET API v2 Subscription-Key, used by scripts/fetch-edinet.ts. Free to
# obtain via EDINET's own API user registration. Optional locally/in CI —
# without it, that script just skips the fetch instead of failing.
EDINET_API_KEY=""
